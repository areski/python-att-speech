.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install python-att-speech

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-att-speech
    $ pip install python-att-speech
